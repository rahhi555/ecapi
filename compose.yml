version: "3.1"

services:
  db:
    image: postgres:15.3
    volumes:
      - db_volume:/var/lib/postgresql/data
      - ./initdb:/docker-entrypoint-initdb.d
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

volumes:
  db_volume:
